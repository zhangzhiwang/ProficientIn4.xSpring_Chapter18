<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.asiainfo.mapper.PostMapper">
	<insert id="save" parameterType="Post">
		insert into t_post (board_id, topic_id, user_id, post_type, post_title, post_text, create_time) values (#{boardId},#{topicId},#{userId},#{postType},#{postTitle},#{postText},#{createTime})
	</insert>
</mapper>